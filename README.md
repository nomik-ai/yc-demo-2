# Classroom Management App

Monorepo with pnpm workspaces. Packages: backend (Express, SQLite, JWT) + frontend (Vite React TypeScript).

## Quick Start

```bash
pnpm install
pnpm setup        # migrate DB + seed demo accounts
pnpm dev          # starts backend:3000 + frontend:5173
```